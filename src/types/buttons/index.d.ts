type ButtonColors = 'default' | 'error' | 'warning';

interface ButtonColorClasses {
  base: string;
  hover: string;
  darkBase: string;
  darkHover: string;
  text: string;
  border: string;
  darkText: string;
  darkBorder: string;
}
