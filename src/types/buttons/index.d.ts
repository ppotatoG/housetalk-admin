type ButtonColors = 'default' | 'error' | 'warning' | 'success';
type ButtonSizes = 'small' | 'medium' | 'large';

interface ButtonColorClasses {
  base: string;
  hover: string;
  darkBase: string;
  darkHover: string;
  text: string;
  border: string;
  darkText: string;
  darkBorder: string;
}
