import Users from '@/component/Users';
import { DefaultLayout } from '@/pageLayout';

const Home = () => {
  return (
    <DefaultLayout>
      <Users />
    </DefaultLayout>
  );
};

export default Home;
