import Dashboard from '@/component/Dashboard';
import { DefaultLayout } from '@/pageLayout';

const Home = () => {
  return (
    <DefaultLayout>
      <Dashboard />
    </DefaultLayout>
  );
};

export default Home;
